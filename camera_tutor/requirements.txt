# Camera Tutor standalone dependencies.
# Use this when deploying camera_tutor on its own (without the english_tutor app):
#   pip install -r camera_tutor/requirements.txt

fastapi>=0.100.0
uvicorn>=0.23.0
httpx>=0.27.0
python-dotenv>=1.0.0
sounddevice>=0.4.0
numpy>=1.24.0
opencv-python>=4.8.0
websocket-client>=1.7.0

# WebRTC remote A/V device mode (camera_tutor/rtc_device.py)
aiortc>=1.13.0

# Local speech pipeline (scripts/local_pipe.py) — optional, local mode only
faster-whisper>=1.1.0
kokoro>=0.9.2
misaki[en]>=0.9.4
soundfile>=0.12.0
